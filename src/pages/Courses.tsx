import { Layout } from "../components/layout";
import { CoursesSummary } from "../components/metrics/CoursesSummary";

const Courses = () => {
  return (
    <Layout>
      <CoursesSummary />
    </Layout>
  );
};

export default Courses;
